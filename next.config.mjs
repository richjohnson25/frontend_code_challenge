/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'd1be5sn7lppxuh.cloudfront.net',
            'annualreport.id',
            'poultry.extension.org',
            'www.cameronsseafood.com',
            'www.mychicagosteak.com'
        ]
    }
};

export default nextConfig;
